# Optional. Where the pad JSON file is written. Defaults to .data/pads.json
# in the working directory. Set to an absolute path (e.g. /var/lib/lan-pad/pads.json)
# if you want it outside the project.
PAD_DATA_FILE=
