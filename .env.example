# Optional. Set these to use Upstash Redis as the shared store (required on Vercel
# / any multi-instance serverless). Leave unset for LAN use — falls back to
# in-memory state (single process, `next start`).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
